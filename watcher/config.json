{
  "tasks": [
    {
      "watch": "./markdown",             
      "command": "npm run convert-md-pandoc && npm run generate-index && npm run generate-sitemap && npm run generate-overview"
    },
    {
      "watch": "./docs",
      "command": "echo 文件有变化"
    }
  ]
}
